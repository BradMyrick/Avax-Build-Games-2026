# ---- General & OS ----
.DS_Store
Thumbs.db
._*
.Spotlight-V100
.Trashes
*.swp
*.swo
*~
*.sublime-workspace

# ---- Secrets & Environment ----
.env
.env.local
.env.*.local
.env.production
.env.development
*.pem
*.key
*.p12
*.pfx
creds.json
secrets.json

# ---- Logs ----
*.log
deploy_logs.txt
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ---- Rust ----
target/
**/*.rs.bk
*.pdb

# ---- Foundry / Solidity ----
# Keep lib/ submodules; ignore build artifacts.
# The two ABI JSONs below ARE committed because amp-relayer and
# amp-integration-tests `abigen!`/`include_str!` them at compile time
# (see Phase 0.1 of the release plan). Re-run `forge build` and recommit
# them whenever contracts/src/*.sol changes.
contracts/out/**
!contracts/out/AMPRegistry.sol/
contracts/out/AMPRegistry.sol/*
!contracts/out/AMPRegistry.sol/AMPRegistry.json
!contracts/out/AMPSettlement.sol/
contracts/out/AMPSettlement.sol/*
!contracts/out/AMPSettlement.sol/AMPSettlement.json
contracts/cache/
contracts/broadcast/

# ---- Editor / IDE ----
.vscode/
.idea/
*.tsbuildinfo

# ---- Build artifacts (all languages) ----
build/
dist/
.cache/
out/
lib/  # generic catch-all; doesn't affect contracts/lib above

# ---- C++ ----
/amp-sdk/examples/cpp/build/
compile_commands.json
*.o
*.obj
*.a
*.lib
*.so
*.dylib
*.dll

# ---- C# ----
**/bin/
**/obj/
*.user
*.suo

# ---- Python ----
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/
.tox/
.coverage
htmlcov/
.slither_venv/
git-pip.py
# Data files used by local runs (relayer-data, amp-data, etc.)
*-data/

# ---- JavaScript / TypeScript ----
**/node_modules/
# napi-rs native build outputs (regenerated by `npm run build:native`):
# the platform-specific .node binary, the JS loader, and the generated .d.ts.
amp-sdk/js/native/*.node
amp-sdk/js/native/amp-native.js
amp-sdk/js/native/amp-native.d.ts

# ---- AMP SDK generated bindings ----
# Generated bindings are produced by `amp-sdk/generate_bindings.sh` and
# by each language's build system. The following patterns hide them from
# version control because they are reproducible from the canonical schemas
# in `amp-sdk/schemas/*.capnp`.
#
# Note: `amp-sdk/schemas/generated/{cpp,csharp}/` IS intentionally tracked
# (these are the outputs that the C++ CMakeLists and the C# .csproj compile
# against, so a fresh clone builds without requiring the capnp compilers to
# be installed).
#
# Note: `amp-sdk/python/amp_sdk/generated/` IS intentionally tracked because
# those are stripped schema SOURCES (not bindings) that ship in the wheel
# and are loaded by pycapnp at runtime.
amp-sdk/csharp/generated/
amp-sdk/go/generated/
amp-sdk/rust/src/generated_*

# ---- Local development data ----
relayer-data/
amp-data/
anvil.log
secrets/
/amp-sdk/cpp/build-tests/
/amp-sdk/cpp/build-cpp-tests/
amp-server/.env
